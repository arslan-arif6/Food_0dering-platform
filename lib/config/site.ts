// Central site/business configuration. Update here once and it propagates
// everywhere the restaurant name/branding is referenced across the app.
export const siteConfig = {
    name: "Home Made Food",
    adminPanelLabel: "Admin Panel",
} as const;